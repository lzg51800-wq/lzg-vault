# 2026-07-12 工作日志

## 飞书多Agent绑定方案设计（方案B）

- 用户选择了方案B：3个独立飞书机器人，每个agent一个独立入口
- 完成了飞书多Agent绑定配置文档，保存到 `🤖AI/飞书多Agent绑定配置(方案B).md`
- 关键发现：
  - openclaw-lark 插件使用 WebSocket 长连接，不需要公网回调URL
  - 多agent通过 `channels.feishu.accounts` 配置多个机器人account
  - ⚠️ **关键修正**：`bindings` 是**顶层字段**（与 `agents` 同级），不在 `session` 下！
  - 正确格式：`{ "agentId": "study", "match": { "channel": "feishu", "accountId": "study" } }`
  - 错误格式：`session.bindings` + `session.defaultAgent`（会导致 `session: Invalid input`）
  - 默认代理通过 `agents.list[].default: true` 标记，不存在 `defaultAgent` 字段
  - 主机器人appId/appSecret放在顶层，其他放在accounts里
  - 需要在飞书开放平台创建3个自建应用，每个开通机器人能力+权限+WebSocket事件订阅
- 配置改动3处：
  1. plugins.entries.openclaw-lark: enabled → true
  2. channels 新增 feishu 配置（3个account）
  3. **顶层** bindings 路由规则（不在session下）
- 用户已在飞书开放平台创建并发布3个应用：投研助理、学习助手、日常问答
- 用户提供3个应用的 App ID 和 App Secret，已填入配置脚本
- 配置方式：Python脚本安全修改JSON（避免手写格式错误）
- ✅ 配置验证通过，网关已重启，等待飞书测试3个机器人路由

## 飞书机器人测试进展

- 飞书开放平台配置要求：每个应用需在「事件与回调」→「事件配置」中
  - 订阅方式选择「使用长连接接收事件」
  - 必须添加 `im.message.receive_v1`（接收消息）事件，否则机器人无法接收消息
- 3个机器人 WebSocket 连接均已启动（日志确认）
- 用户完成事件配置后测试结果：
  - ✅ 日常问答：正常回复 `1+1 = 2`
  - ✅ 学习助手：正常回复递归解释
  - ✅ 投研助理：正常输出中国银行股票分析报告（调用 westock-data 等金融工具）
- **结论：飞书多 Agent 绑定（方案B）配置成功，3个机器人独立运行**

## 配置文档存档修正

- 更新 `🤖AI/飞书多Agent绑定配置(方案B).md`：修正文档中错误的 `session.bindings`/`session.defaultAgent` 格式为验证通过的顶层 `bindings` 格式
- 修正 5 处：核心原理、配置脚本、配置说明表、故障排查表、附录 JSON
- 新增「最终配置状态」节：验证结果、验证流程、8条关键踩坑
- 文档现已与服务器实际生效配置一致，可作为未来参考存档

## 静态网站部署（CloudStudio）

- 用户原本想 SSH 登 `website@124.220.224.124`（腾讯云同机 website 账号）自建 Nginx 部署静态站
- 排错：私钥 `~/Desktop/website_key` 末行缺换行导致 SSH `invalid format`，`printf '\n' >>` 补换行后恢复，成功登录
- 用户放弃在「同服务器」部署（"不在一个服务器跑了"），也不想在本地建项目文件（"不准备建立在本地"）
- 改用 **CloudStudio 云端部署**：用户给 `/Users/Apple/Downloads/index.html`（自包含单文件，CSS/JS 全内联，无外部依赖）
- 复制到 `/tmp/aily-site/index.html`（临时目录，不污染本地项目），调用 `workbuddy_cloudstudio_deploy` 部署成功
- 分享链接：`https://3f085ece56384ca5815bfadeead2b762.app.codebuddy.work`（沙箱ID 3f085ece56384ca5815bfadeead2b762）
- 页面内容：「本周 AI 热点速览 2026-07-12」，8 条 AI 新闻卡片 + 分类/影响力筛选，纯前端

## 龙虾自动生成 AI 周刊 → COS 静态托管（方案B 落地）

- 用户确认方案B：让腾讯云 CVM 上的 OpenClaw（龙虾，ubuntu 账号）每天自动生成 `index.html` 并上传到腾讯云 COS 静态托管，固定公网 URL 每天更新
- 飞书里的「liang的智能伙伴」做不到自动部署（只能 IM 发文件），故由龙虾做
- 已产出完整脚本套件，存于 `🤖AI/龙虾AI周刊自动部署/`：
  - `render_ai_weekly.py`：读 `news.json` → 渲染 `index.html`，**原样保留原 CSS/JS，只换 data 数组+日期**；用 `__XXX__` 占位符 + `.replace()` 避免 f-string 与 JS 模板 `${...}` 冲突（已本地验证通过）
  - `upload.sh`：读 `.env` 的 `COS_BUCKET` → `coscli cp` 上传
  - `setup_cos.sh`：装 coscli + 打印凭证配置说明
  - `cron_prompt.txt`：交给龙虾 agent 的定时任务提示词（搜新闻→写 news.json→render→upload）
  - `README.md`：完整部署指南（含用户在腾讯云控制台需建的 COS bucket + 静态托管 + API 密钥）
- 龙虾 cron 用 CLI 最稳：`openclaw cron add --name ai-weekly --cron "0 18 * * *" --tz Asia/Shanghai --session isolated --agent main --message "$(cat cron_prompt.txt)"`
- 关键约束：WorkBuddy 无法 SSH 到 ubuntu 账号（只有 website_key 用于 website 账号），故全部脚本需用户在龙虾终端执行
- 已将「OpenClaw cron→生成静态站→上传 COS」可复用流程存为 skill：`openclaw-cron-static-site`

## 架构决策：AI 周刊部署不需要单独开服务器

- 用户问是否单独开一台 CVM 更好 → 分析后结论：纯静态站不需要，COS 静态托管已与龙虾 CVM 基础设施隔离、且零服务器攻击面
- 用户核心诉求是「安全隔离」，但明确选择**方案 B 原版**（COS 静态托管），**不做 CAM 子账号分权那层**——保持简单
- 最终架构定案：龙虾 cron(每天18:00) → 生成 news.json → render_ai_weekly.py → upload.sh 上传 COS → 固定公网 URL 自动更新

## COS 存储桶创建完成

- 用户在腾讯云控制台创建 COS 存储桶：`ai-weekly-1438898998`
- 地域：`ap-guangzhou`（广州）
- 静态网站托管已开启：默认首页 `index.html`，默认 404 页 `index.html`
- 固定公网 URL：`https://ai-weekly-1438898998.cos-website.ap-guangzhou.myqcloud.com`
- 下一步：获取 CAM API 密钥（SecretId/SecretKey），在龙虾服务器上配置 `coscli` 与 `.env`

## coscli 安装 + setup_cos.sh 地址修正

- 用户登录龙虾 `ubuntu` 账号（密码登录，非密钥），4 个脚本已 `scp` 到 `/home/ubuntu/ai-weekly/`
- 原 `setup_cos.sh` 里 coscli 下载地址失效（404）：旧地址 `...coscli-linux-amd64.zip`（多了 `.zip` 后缀且是 zip 包但实际是二进制）
- 修正为官方二进制直链：`https://cosbrowser.cloud.tencent.com/software/coscli/coscli-linux-amd64`，改为直接 `curl` 下载 → `chmod 755` → `install` 到 `/usr/local/bin/coscli`（不再 unzip）
- 用户在服务器直接用手动命令安装成功：`coscli version v1.0.8` ✅
- 当前卡在最后一步：用户需在龙虾填 `coscli config set secretid/secretkey` + 写 `.env`(COS_BUCKET=ai-weekly-1438898998) + 占位页 `bash upload.sh` 测上传链路
- ⚠️ coscli 若不支持 `config set` 子命令，备选是直接写 `~/.cos.yaml` 配置文件（secretid/secretkey/region: ap-guangzhou/buckets 别名）
- **实测**：龙虾上 `coscli v1.0.8` 运行 `coscli config set secretid xxx` 会触发**交互式配置向导**（提示 "Input Your Mode:"），证实该版本无 `config set` 子命令。已改用直接写 `~/.cos.yaml` 方式配置（格式：cos.base.secretid/secretkey/sessiontoken + cos.buckets 列表含 name/alias/region），绕过交互。
- **安全操作建议**：配置 COS 密钥时推荐用 `vi ~/.cos.yaml` 直接编辑（密钥不进 shell 历史），优于 `cat >` 重定向（密钥会留在 bash history）。密钥由用户自己填、不进聊天（用户选方式 B）。
- **坑**：coscli 交互式配置向导（`coscli config` 不带参数触发）会自动生成一个**损坏的** `~/.cos.yaml`（含垃圾字段如随机 sessiontoken、buckets.name: he/alias: 'ile:'/region 空），用户 vi 打开看到乱码。救援：vi 里 `Esc` + `:q!` 不保存退出，再用 `cat > ~/.cos.yaml << 'EOF' ... EOF` 强制覆盖正确内容（secretid/secretkey/sessiontoken:"" + buckets name/alias/region:ap-guangzhou），不要走交互向导。

## ⏸ COS 部署暂停（2026-07-12 17:38）

- **用户决定**：先暂停 COS 部署，先搞通「本地便捷 SSH 连接服务器」的方法，搞通后再继续布置
- **当前暂停时的状态**：
  - ✅ 脚本已 scp 到 `/home/ubuntu/ai-weekly/`（render/upload/setup/cron_prompt）
  - ✅ coscli v1.0.8 已装好
  - ✅ COS 桶 `ai-weekly-1438898998` + 静态托管已开，URL 就绪
  - ❌ `~/.cos.yaml` 仍是 coscli 向导生成的**损坏文件**（非真实密钥），真实 SecretId/SecretKey 尚未正确写入
  - ❌ 上传链路未验证（upload.sh 未成功跑过）
- **恢复时执行顺序**（用户搞定 SSH 后）：
  1. `ssh lobster`（新别名，见 SSH 便捷配置）
  2. `cat > ~/.cos.yaml << 'EOF' ... EOF` 强制覆盖，填**真实** SecretId(AKID开头)/SecretKey
  3. `cd /home/ubuntu/ai-weekly && echo "COS_BUCKET=ai-weekly-1438898998" > .env && chmod 600 .env`
  4. `echo "<h1>ai-weekly placeholder</h1>" > index.html && bash upload.sh`
  5. 浏览器开 `https://ai-weekly-1438898998.cos-website.ap-guangzhou.myqcloud.com` 看占位页
  6. 通了再加 cron：`openclaw cron add --name ai-weekly --cron "0 18 * * *" --tz Asia/Shanghai --session isolated --agent main --message "$(cat cron_prompt.txt)"`

## ✅ 本地 SSH 便捷登录搞定（2026-07-12 17:45）

- 在 Mac 本地生成 ed25519 密钥：`ssh-keygen -t ed25519 -f ~/.ssh/lobster_key -N "" -C "lobster-ubuntu"`
- 创建 `~/.ssh/config` 写入 `Host lobster` 别名（HostName 124.220.224.124 / User ubuntu / IdentityFile ~/.ssh/lobster_key / StrictHostKeyChecking no / UserKnownHostsFile /dev/null）
- `ssh-copy-id -i ~/.ssh/lobster_key.pub lobster` 推公钥（需输一次密码），之后 `ssh lobster` 免密登录成功
- ⚠️ `ssh-copy-id lobster`（不带 `-i`）会报 "No identities found"，必须显式指定 `.pub` 文件

## 🔄 决策反转：COS 改用 CAM 最小权限子账号（2026-07-12 17:46）

- 用户在上面第70-72行曾决定"不做 CAM 子账号，保持简单"。现**反转**为：建立腾讯云 CAM 子账号（一次配置），只授予 `ai-weekly-1438898998` 桶的最小 COS 权限，用子账号 SecretId/SecretKey 配置 coscli
- 动机：密钥泄露面小、权限隔离干净，主账号安全
- 待执行：控制台建子用户(编程访问)→自定义策略(仅该桶 PutObject/GetObject/HeadObject/ListBucket)→建 API 密钥→拿到子账号 SecretId/SecretKey→填 `~/.cos.yaml`
- 桶 APPID = `1438898998`，resource 格式：`qcs::cos:ap-guangzhou:uid/1438898998:ai-weekly-1438898998/*` 与 `qcs::cos:ap-guangzhou:uid/1438898998:ai-weekly-1438898998`
- ✅ **子账号密钥写入 ~/.cos.yaml 成功，upload.sh 上传成功**（18:18，`index.html` 34B 上传到 cos://ai-weekly-1438898998/index.html，PutObject 权限 OK）
- ⚠️ `coscli ls` 报 403：因策略缺 `cos:HeadBucket`（ls 会对桶根发 HEAD 请求）；上传走 PutObject 不受影响。可选补 `cos:HeadBucket` 到策略 action 列表，使 ls 也能用
- 实测 CAM 自定义策略 action 列表里 `cos:ListBucket` **不存在**（报错），应改用 `cos:GetBucket`/`cos:HeadBucket`
- 子账号策略最终 action：`cos:PutObject, cos:GetObject, cos:HeadObject, cos:HeadBucket, cos:GetBucket`（resource 为桶名 + 桶名/* 两行）
- ⚠️ **浏览器首次打开 URL 时提示下载**：因为 coscli `cp` 默认把 Content-Type 设为 `application/octet-stream`，Safari 当成文件下载。修复：上传时加 `--meta "Content-Type:text/html"`，已更新 `upload.sh` 并重新上传占位页

## ✅ AI 周刊全链路闭环完成（2026-07-12 18:30）

- **cron 创建成功**：`openclaw cron add --name ai-weekly --cron "0 18 * * *" --tz Asia/Shanghai --session isolated --agent main --message "$(cat cron_prompt.txt)"`，`openclaw cron list` 可见，下次跑 7/13 18:00
- **Tavily 插件缺失（关键坑）**：cron 提示词第1步用 Tavily 搜索，但 `openclaw.json` 里 `plugins.entries.tavily` 虽 enabled 且有 apiKey，**插件包本体没装**，启动时报 "plugin not installed: tavily" → cron 跑第1步会失败。修复：`openclaw plugins install @openclaw/tavily-plugin` → 重启 gateway 加载
- **Gateway 重启操作（服务器）**：`ps aux | grep "openclaw.*gateway"` 找 PID → `kill <pid>` → `setsid bash -lc "openclaw gateway run --force" > ~/openclaw-gateway.log 2>&1 < /dev/null &`（`setsid` 脱离终端，SSH 断开不死）。node 路径 `/home/ubuntu/.nvm/versions/node/v22.22.3/bin`，非交互 SSH 跑 openclaw CLI 必须 `bash -lc` 否则 `node: not found`
- **端到端空跑通过**：造 3 条合规 news.json → `python3 render_ai_weekly.py` 输出 "index.html generated: 3 条" → `bash upload.sh` 上传 9KB → 线上 `curl` 抓到「本周 AI 热点速览」→ 恢复占位页。整条链路无问题
- **已知小尾巴**：ai-weekly 的 delivery 是 `announce -> last`（"no route, fail-closed"），即 cron 跑完的「生成几条/上传成功」小结**不会推到微信**。核心生成+上传不受影响；若要微信通知需改 `--channel openclaw-weixin`（与其他 cron 一致），但改动有风险，本次未动
- 最终固定 URL：`https://ai-weekly-1438898998.cos-website.ap-guangzhou.myqcloud.com`

## 🔍 重要纠正：CLI 的 "plugin not installed: tavily" 是误报

- 用户在交互式终端跑 `openclaw cron list` 仍看到 `plugins.entries.tavily: plugin not installed: tavily` 警告，一度以为修复失败
- **真相**：该警告是 CLI 配置校验的**误报**。CLI 用 `require.resolve('@openclaw/tavily-plugin')` 校验，但它不搜索 `~/.openclaw/npm/projects/` 插件目录，所以报"not installed"；而真正跑 cron 的 **gateway 用另一套插件加载器，会扫描该目录**，已成功加载 tavily
- 旁证：① 管道模式（`cron list | head`）下 OpenClaw 压制横幅+警告，所以之前我的检查"看不到警告"是假象；② 交互式 TTY 才打印完整横幅+警告
- **决定性验证**：手动触发真实运行 `openclaw cron run a520469d-7c2f-4015-a3b1-88477fbd099d`（⚠️ `cron run` 要 job 完整 ID，不是 `--name` 的 `ai-weekly`），18:36 成功生成真实 news.json（8 条：模型×3/政策×2/资本/终端/产品）→ 渲染 13871 字节 index.html → 上传 COS → 线上「本周 AI 热点速览」显示真实内容，首条标题「OpenAI 发布 GPT-5.6...」已嵌入 HTML
- **结论**：Tavily 在 gateway 运行时可用，明晚 18:00 自动跑必定成功。CLI 警告可安全忽略，无需再处理
- 若想消除该误报：需把插件 symlink 进 openclaw 的 node_modules（`/home/ubuntu/.local/share/pnpm/global/5/.pnpm/openclaw@2026.6.11/node_modules/@openclaw/tavily-plugin`），但动的 pnpm 全局 store 有风险，且不影响功能，故不动

## 📄 今日工作简报存档（2026-07-12 18:41）
- 汇总当日全部工作（飞书多 Agent + AI 周刊自动部署）为一份简报，存档到 `🤖AI/工作简报-2026-07-12.md`
- 用户确认 ai-weekly cron 不需要改微信通知，维持 `announce -> last`

## 🗂 Obsidian 知识库骨架搭建（2026-07-12 18:50）

- 用户提出"用 Obsidian 建立知识库"需求，给出方法论（PARA + 双链 + MOC + 插件三层架构），用户确认执行"阶段一·搭骨架"
- 在 vault 根目录新建 2 个文件夹：`Templates/`（模板）、`Inbox/`（未整理碎片）
- 写 3 套笔记模板（Obsidian 核心 Templates 插件变量 `{{date:YYYY-MM-DD}}`）：
  - `Templates/每日笔记模板.md`：今日要点/待办/灵感/关联
  - `Templates/概念笔记模板.md`：定义/要点/关联概念`[[ ]]`/来源（标题用 `{{title}}` 占位，插入后手动改）
  - `Templates/文献笔记模板.md`：出处/核心观点/我的思考/标签
- 写 2 份 MOC 索引（用 `[[双链]]` 聚合现有真实笔记）：
  - `💰金融/MOC-金融.md`：分区=个股分析/晨报早间/收盘复盘/新闻简报/宏观策略/工具配置；已双链 A股早间简报(2篇)+金融新闻简报(2篇)+重大新闻事件简报
  - `🤖AI/MOC-AI.md`：分区=模型技术/工具配置部署/自动化脚本/工作记录/学习笔记；已双链 龙虾定时任务配置/龙虾多Agent架构部署/龙虾新增任务配置/OpenClaw部署与配置指南/飞书多Agent绑定配置(方案B)/工作简报-2026-07-12
- 阶段二三待用户手动：给旧笔记补双链、登记 MOC、装 Dataview/QuickAdd/Templater/Obsidian Git 等插件
- 注：概念/文献模板标题用 `{{title}}` 占位（核心 Templates 插件不支持 title 变量，需插入后手改；若装 Templater 可改 `<% tp.file.title %>`）

## 🔌 知识库插件 + Git 备份落地（2026-07-12 19:40-19:55）

- 用户在 Obsidian 社区插件装好 4 个：Dataview / Git / QuickAdd / Templater（已启用）；核心 Templates 也已配置"模板文件夹位置=Templates"
- **模板升级为 Templater 语法**（原核心模板 `{{date:YYYY-MM-DD}}`/`{{title}}` 改为自动填充）：
  - `{{date:YYYY-MM-DD}}` → `<% tp.date.now("YYYY-MM-DD") %>`
  - `{{title}}` → `<% tp.file.title %>`
  - 三套模板已全部改写
  - Templater 需设"Template folder location = Templates"（用户需在 Obsidian 设置里点）
- **Obsidian Git 备份闭环**：
  - 在 vault 根目录 `git init`（WorkBuddy 沙盒执行，落真实 Mac 路径）+ 写 `.gitignore`（忽略 .obsidian/workspace.json、workspace-mobile.json、cache、.trash/、.DS_Store）
  - 首次 commit `978d83c` "chore: init vault with Obsidian Git backup"
  - GitHub 专用 SSH key：`ssh-keygen -t ed25519 -f ~/.ssh/github_key -N "" -C "github-lzg-vault"`（在**用户 Mac 终端**跑，沙盒对 ~/.ssh 写受限）。`~/.ssh/config` 加 `Host github.com / IdentityFile ~/.ssh/github_key`
  - 公钥 `github_key.pub` 已加进 GitHub 账户（key 名 MacBook-Pro），GitHub 仓库 `lzg51800-wq/lzg-vault`（Public，空仓库创建）
  - remote 用 SSH 地址：`git@github.com:lzg51800-wq/lzg-vault.git`（虽用户给的是 HTTPS 地址，转 SSH 更省事，免密）
  - 首次 push 已成功，GitHub 上 70 个文件齐全，remote main = 978d83c
  - 用户在 Obsidian Git UI 把 "Auto commit-and-sync interval" 设为 30 并保存，生成 `data.json`（`autoSaveInterval:30, disablePush:false, pullBeforePush:true, commitMessage:"vault backup: {{date}}"`）
  - WorkBuddy 用命令补写仓库级 author：`git config user.name "Lzg"` / `git config user.email "local@lzg.local"`（消除 "Git is not ready" 提示，UI 里找不到 Author 字段，故命令行写入）
  - 手动验证 commit+push 成功：`978d83c..40f4dd5 main -> main`，GitHub 上最新 commit = 40f4dd5
  - **结论**：Obsidian Git 自动备份全链路闭环，每 30 分钟静默 commit+push 到 GitHub，无需用户再操作（仅建议重启 Obsidian 刷新 "Git is not ready" 缓存）
- 关键提醒：GitHub 验证（sudo mode）走邮箱 6 位验证码；SSH key 必须在用户自己 Mac 终端生成（沙盒写 ~/.ssh 被拒 "Operation not permitted"）；Obsidian Git 2.38.6 的自动备份字段叫 "autoSaveInterval"（UI 显示 "Auto commit-and-sync interval"），不是旧版的 "Vault backup interval"

## 📚 知识库阶段二：补层级标签 + MOC 动态化（2026-07-12 20:20-20:25）

- **11 篇旧笔记统一层级标签**：
  - 金融 5 篇 frontmatter 扁平标签→层级：新闻简报=`金融/新闻简报`、A股早间=`金融/A股`、重大事件=`金融/重大事件`
  - AI 6 篇原无 frontmatter，新增：OpenClaw部署=`AI/部署`、龙虾定时/新增任务=`AI/自动化`、龙虾多Agent=`AI/架构`、飞书=`AI/飞书`、工作简报=`AI/工作记录`
- **两份 MOC 升级为 Dataview 动态汇总**：各区改为 `TABLE ... FROM #标签 SORT created DESC` 查询，新笔记打标签即自动出现；保留「⭐精选」手动双链区 + 「全部笔记」FROM 文件夹汇总
- 修正 MOC frontmatter 里残留的 `created: {{date:YYYY-MM-DD}}` 未替换变量→实际日期 2026-07-12
- 已 commit+push GitHub：`5e7ce94..4b3a482`
- 标签体系约定（后续新笔记沿用）：金融=`金融/{A股|新闻简报|重大事件|个股|宏观|策略}`；AI=`AI/{架构|部署|自动化|飞书|模型|工作记录}`

## 🏠 知识库总入口 Home.md 落地（2026-07-12 20:26）

- 新建 vault 根目录 `🏠 Home.md` 作为知识库总入口，聚合两个 MOC + Inbox + Templates
- 内容：双链 `[[MOC-金融]]`/`[[MOC-AI]]`、Inbox/Templates 仅作纯文本（文件夹不能 wikilink）、两个 Dataview 区块（最近新增/最近修改全库，均 `WHERE file.name != this.file.name` 排除自身）、标签地图、使用提示
- tags: [home]；明确提示 Dataview 在阅读模式（Cmd+E）查看渲染
- 已 commit+push GitHub：`4b3a482..d76cd25`（commit d76cd25「新增 🏠 Home.md 总入口」），备份闭环完整
- 建议：在 Obsidian 设置里把 Home.md 设为「默认新标签页」或起始页，体验更佳（待用户自行设置）

## ⚙️ 知识库首页固化：安装 Homepage 插件（2026-07-12 20:30-20:32）

- 用户说"你帮我做"→ 把 🏠 Home.md 设为 Obsidian 启动默认页
- 方案：装官方 **Homepage** 社区插件（mirnovov/obsidian-homepage，最新 4.4.4），从 GitHub Release 直链下载 main.js/manifest.json/styles.css 到 `.obsidian/plugins/homepage/`（沙盒可联网下载，绕过 Obsidian 内置社区插件市场）
- 写 `data.json`（version:4）：`homepages["Main Homepage"]` = `{ kind:"File", value:"🏠 Home", openOnStartup:true, openWhenEmpty:true, view:"Reading view", refreshDataview:true, openMode:"Replace all open notes", revertView:true }`
- 在 `community-plugins.json` 追加 `"homepage"` 启用（现 8 个插件）
- ⚠️ **关键时序坑**：Obsidian 运行时退出的瞬间会用内存里的旧插件列表**覆盖** community-plugins.json，抹掉刚加的 homepage。因此必须让用户先 **Cmd+Q 完全退出**，确认退出后我才锁定配置；用户退出后验证两个文件均未被覆盖（homepage 仍在启用列表、data.json 完好）
- 用户已退出并确认；下一步用户重开 Obsidian 即自动停在 🏠 Home（阅读模式，Dataview 渲染）
- 注：Homepage 4.4.4 要求 Obsidian ≥ 1.12.2；用户已装 templater/quickadd/dataview 等较新插件，版本应满足。插件目录在 Git 追踪范围内（.gitignore 只忽略 workspace.json/cache），本应随下次 Obsidian Git 自动备份；但为保险可手动 commit（未做，待下次自动备份或用户确认）
- 反思：Obsidian 插件用"下载文件+手写 data.json+改 community-plugins.json"的方式可在无 GUI 市场时离线安装，比让用户手动搜插件更稳；Homepage 的 data.json 结构是 version+homepages 嵌套（非扁平），写错字段会加载失败，需先 grep main.js 确认字段名/枚举值

## A股早间简报自动生成（2026-07-12 23:55）

- 自动化任务 `automation-1780959215097` 触发，生成覆盖 7/13 周一开盘的早间简报
- 输出文件：`💰金融/A股早间简报-2026-07-12.md`
- 🔴 **重大突发纳入**：7/12凌晨伊朗宣布关闭霍尔木兹海峡（全球21%石油贸易中断），油价暴涨8.7%（布伦特98.24/WTI94.71），VLCC运费翻倍，为1970年代以来最严重石油供应冲击
- 简报核心策略调整为"谨慎偏防御"，总仓位上限40%，三主线调整为：石油石化/油运（地缘催化）、创新药（防御性进攻）、商业航天/军工（长征十号乙+地缘双重驱动）
- 7/11简报复盘：商业航天命中、创新药命中、AI算力偏差、高低切换逻辑准确
- 已更新 automation memory.md 执行记录

